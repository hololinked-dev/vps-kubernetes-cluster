tls:
  - hosts:
    - cluster.example.com
    secretName: example-com-tls

rules:
- host: cluster.example.com
  http:
    paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kubernetes-dashboard-web
            port:
              number: 8000
